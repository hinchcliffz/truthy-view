// import lib from './lib'
import lib from './lib'

const TruthyView = {
  // Class Name
  name: 'TruthyView',
  // Client tools
  clientTools: lib.clientTools,
  // Encryption
  encryption: lib.encryption,
  // Flat
  flat: lib.flat,
  // Mac Address
  macAddress: lib.macAddress,
  // Trace
  trace: lib.trace,
  // Save
  storage: lib.storage,
}
module.exports = TruthyView
